import cartLogo from './shopping-cart.png'

const CartWidget = () => {
    return  (
        <div>
            <img src={cartLogo} alt="Cart-Widget" />
            {0}
        </div>
    )
}

export default CartWidget